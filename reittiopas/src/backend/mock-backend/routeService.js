export const getFastestRoute = (start, end) => {
    return (['A', 'B', 'C']);
}

export const getLinesForRoute = (route) => {
    return ([
        { name: "keltainen", stops: ['A', 'B']},
        { name: "punainen", stops: ['C'] }
    ]);
}